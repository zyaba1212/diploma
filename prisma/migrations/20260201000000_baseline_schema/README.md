# Baseline schema (squashed)

Единая начальная миграция, сгенерированная из текущего `prisma/schema.prisma` (`prisma migrate diff --from-empty --to-schema-datamodel`).

Ранее в репозитории не было миграции, создающей базовые таблицы (`Proposal` и др.) — только инкрементные шаги Stage 6–12, из‑за чего `migrate deploy` на пустой БД падал.

Исторические папки `20260319_*` / `20260320_*` удалены как дублирующие текущую схему.
