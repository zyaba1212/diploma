name: CI

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  lint-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # package-lock.json is generated with npm 11; default npm on Node 22 (npm 10)
      # reports package.json / lock out of sync and fails `npm ci`.
      - name: Align npm with lockfile
        run: npm install -g npm@11.8.0

      - name: Install dependencies
        run: npm ci

      - name: Prisma generate
        run: npm run prisma:generate

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Dependency audit (informational)
        run: npm audit --audit-level=high
        continue-on-error: true
